# R
.Rproj.user/
.Rhistory
.RData
.Ruserdata
*.Rproj

# renv manages its own renv/.gitignore (ignores library/, staging/, etc.);
# renv.lock, .Rprofile and renv/activate.R are meant to be committed.
packrat/

# OS
.DS_Store
Thumbs.db

# Claude Code / agent tooling (not part of the project)
.claude/
.agents/

# pkgdown site is built and deployed by CI, not committed
docs/
