% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read.R
\name{read_pgn}
\alias{read_pgn}
\title{Read a game from PGN}
\usage{
read_pgn(x)
}
\arguments{
\item{x}{PGN text, or a path to a \code{.pgn} file.}
}
\value{
A \link{tanmai_game} data frame, one row per ply, with the PGN's header
tags attached as a \code{headers} attribute.
}
\description{
Returns one row per ply - the shape everything else in the package reads.
Comments, annotation glyphs and variations are tolerated, and a \verb{[FEN]}
header is honoured, so a study or an endgame is not silently replayed from
the standard opening position.
}
\details{
The evaluation columns (\code{cp}, \code{cp_loss}, \code{best_uci}, \code{best_san}, \code{class})
come back as \code{NA}. Filling them needs an engine and is \code{\link[=evaluate]{evaluate()}}'s job.
}
\examples{
game <- read_pgn("1. e4 e5 2. Nf3 Nc6 3. Bb5 a6")
game[, c("ply", "side", "san", "uci")]
}
