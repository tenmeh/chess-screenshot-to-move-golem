% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_engine_session.R
\name{engine_session_eval}
\alias{engine_session_eval}
\title{Evaluate one position on a persistent engine, waiting for the result}
\usage{
engine_session_eval(sess, fen, movetime_ms = 300L)
}
\arguments{
\item{sess}{An engine session from \code{\link[=engine_session_start]{engine_session_start()}}.}

\item{fen}{The position to evaluate.}

\item{movetime_ms}{Thinking time.}
}
\value{
A list with \code{cp} (centipawns from the side to move's point of view,
mate scores folded onto the same scale), \code{best} (best move in UCI) and
\code{depth}, or \code{NULL} if no session or no result.
}
\description{
The counterpart to \code{\link[=engine_session_analyse]{engine_session_analyse()}}: a bounded, blocking search
for callers that need a number now rather than a stream of improving ones.
Scoring the moves of a live game needs exactly that - one figure per
position, cheap enough to run as the game goes on.
}
\details{
Give this its own session rather than sharing the board's. The board runs
\verb{go infinite}, and interrupting it here would restart its search and reset
its depth on every move of the game.
}
