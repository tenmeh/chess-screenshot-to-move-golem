% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_recognize.R
\name{recognize_symbols_auto}
\alias{recognize_symbols_auto}
\title{Recognize 64 squares against all available sets at once}
\usage{
recognize_symbols_auto(squares, libs = load_all_template_libraries())
}
\arguments{
\item{squares}{A list of 64 magick square images, row-major, top row first.}

\item{libs}{Named list of template libraries to score against (default:
every locally available set).}
}
\value{
A list with \code{symbols} (from the winning set), \code{set}, \code{scores}
(per-set mean similarity), \code{margin} (winner minus runner-up), \code{min_occ}
(worst occupied-square match) and \code{confident} (whether both gates pass).
}
\description{
Scores every square against every template of every installed set in a
single matrix multiply, then picks the best-matching set and reports
confidence signals (see the inline comments for how they are calibrated).
}
