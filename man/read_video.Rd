% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/video.R
\name{read_video}
\alias{read_video}
\title{Read a game from a recording of it being played}
\usage{
read_video(
  path,
  fps = 1,
  region = NULL,
  start = c("start", "first"),
  turn = c("w", "b"),
  max_plies = 2L,
  piece_sets = NULL,
  verbose = FALSE
)
}
\arguments{
\item{path}{A video file, an animated GIF, or a directory of stills.}

\item{fps}{Frames to sample per second. One is usually plenty - it only has
to be faster than the players are moving, not faster than the animation.}

\item{region}{Optional \code{c(x, y, width, height)} in pixels, if the board is
part of a larger screen. By default each frame is auto-cropped.}

\item{start}{Where the game begins: \code{"start"} for the standard position, or
\code{"first"} to anchor on whatever the first readable frame shows.}

\item{turn}{Side to move at the anchor. Ignored when \code{start = "start"}.}

\item{max_plies}{Longest move sequence that may be inferred between two
frames. Two allows a frame to be missed entirely; more invites guessing.}

\item{piece_sets}{Template libraries to match against. Defaults to every set
installed locally.}

\item{verbose}{Report progress while working through the frames.}
}
\value{
A \link{tanmai_game}, with a \code{ledger} attribute recording what happened
to every frame that was looked at.
}
\description{
Takes a screen recording, an animated GIF, or a folder of screenshots, and
returns the game that was played - the same \link{tanmai_game} that \code{\link[=read_pgn]{read_pgn()}}
returns, so everything else in the package works on it unchanged.
}
\details{
Nothing seen is trusted on its own. A frame joins the game only when exactly
one legal sequence of moves explains it, which is what makes this work on
real footage: a recording is full of frames showing a piece halfway through
a slide, or the board mid-redraw, and those either explain nothing legal or
reproduce a position the game has already been in. Both are discarded. The
count of what was accepted and what was thrown away, and why, comes back as
a \code{ledger} attribute rather than being hidden.
}
\examples{
\donttest{
# A folder of screenshots, one per move, works as well as video and needs
# nothing installed:
# game <- read_video("~/screenshots/my-game/")
# accuracy(evaluate(game))
}
}
