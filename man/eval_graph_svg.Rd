% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/golem_utils.R
\name{eval_graph_svg}
\alias{eval_graph_svg}
\title{Draw the evaluation across a game as an inline SVG}
\usage{
eval_graph_svg(cp, click_input = NULL, width = 420, height = 110)
}
\arguments{
\item{cp}{Centipawn evaluations from White's point of view, one per position
(so one more than the number of moves). \code{NA} where not yet measured.}

\item{click_input}{Namespaced Shiny input id to report clicks to, or \code{NULL}
for a static graph. Each point reports its own index as \code{ply}, counting
the moves played to reach it - so 0 is the opening position, and every
other value names the move that arrived there.}

\item{width, height}{Pixel size of the graph.}
}
\value{
A \code{shiny::tags$svg} element, or \code{NULL} if there is nothing to draw.
}
\description{
The drawing half of the evaluation graph; \code{\link[=eval_graph_geometry]{eval_graph_geometry()}} does the
arithmetic. This lives in the app layer rather than beside the tracker
because it is the one place a UI toolkit is needed, and the core is kept
free of one - see \code{test-core-boundary.R}.
}
