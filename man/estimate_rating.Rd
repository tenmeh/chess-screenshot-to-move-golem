% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_rating_estimate.R
\name{estimate_rating}
\alias{estimate_rating}
\title{Estimate an opponent's rating from the moves they played}
\usage{
estimate_rating(pool, fens, ucis, side = NULL)
}
\arguments{
\item{pool}{A pool from \code{\link[=maia_pool_start]{maia_pool_start()}}.}

\item{fens}{Positions before each move, as recorded by the game tracker.}

\item{ucis}{Moves played, aligned with \code{fens}.}

\item{side}{Which side to model, "w" or "b"; \code{NULL} scores every move.}
}
\value{
A list with \code{posterior} (named, sums to 1), \code{rating} (the MAP
estimate), \code{n_moves} scored, \code{evidence} in nats, and \code{loglik} per rating.
}
\description{
Scores every move the given side played under each Maia network and returns
the posterior over ratings.
}
\details{
\code{evidence} is the part worth reading before the estimate. It is the summed
spread of per-move log-probabilities across networks, in nats: how much the
observed moves actually distinguished one network from another. A game of
forced recaptures and obvious developing moves can run twenty plies and
carry almost none, because every network would have played the same thing.
Move count alone does not tell you that.
}
