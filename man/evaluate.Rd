% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate.R
\name{evaluate}
\alias{evaluate}
\alias{evaluate.tanmai_game}
\alias{evaluate.tanmai_position}
\title{Evaluate a position or a game with a chess engine}
\usage{
evaluate(x, ...)

\method{evaluate}{tanmai_game}(x, movetime = 300L, engine_path = NULL, verbose = FALSE, ...)

\method{evaluate}{tanmai_position}(x, movetime = 1000L, engine_path = NULL, ...)
}
\arguments{
\item{x}{A \link{tanmai_game} or \link{tanmai_position}.}

\item{...}{Unused.}

\item{movetime}{Milliseconds of thinking time per position. The default is
deliberately small: a game is many positions, and doubling this doubles the
wait.}

\item{engine_path}{Path to a Stockfish binary. Found automatically if \code{NULL}.}

\item{verbose}{Report progress while working through a long game.}
}
\value{
\code{x} with the engine columns filled in.
}
\description{
For a game this fills in the columns \code{\link[=read_pgn]{read_pgn()}} leaves empty: \code{cp},
\code{cp_loss}, \code{best_uci}, \code{best_san} and \code{class}. For a position it returns the
evaluation and the best move.
}
\details{
\code{cp} is always from \strong{White's} point of view, so the sign means the same
thing down the whole column and the evaluation graph does not flip every
ply. \code{cp_loss} is from the \strong{mover's}, because "you lost 3 pawns" is only
meaningful that way for both colours.
}
\examples{
\donttest{
if (has_engine()) {
  game <- evaluate(read_pgn("1. e4 e5 2. Nf3 Nc6 3. Bb5 a6"))
  game[, c("san", "cp", "cp_loss", "class")]
}
}
}
