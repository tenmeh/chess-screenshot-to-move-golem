% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_game_tracker.R
\name{track_observation}
\alias{track_observation}
\title{Decide what a freshly recognized position means for a tracked game}
\usage{
track_observation(ctx, game, observed_fen, max_plies = 2L)
}
\arguments{
\item{ctx}{A chess.js V8 context from \code{\link[=new_chess_context]{new_chess_context()}}.}

\item{game}{A game record from \code{\link[=game_new]{game_new()}}.}

\item{observed_fen}{The FEN just recognized; only its placement is used.}

\item{max_plies}{Longest move sequence that may be inferred at once.}
}
\value{
A list with \code{status}, \code{moves} (UCI moves for status \code{move}),
\code{anchor_fen} (the position the moves are played from - the current one,
unless the turn had to be flipped), \code{turn_flipped} and a human-readable
\code{reason}.
}
\description{
The whole point of the feature lives here. An observation is classified, not
trusted:
}
\details{
\itemize{
\item \code{invalid} - the recognizer produced nothing usable.
\item \code{unchanged} - the same position we already hold; the common case, and not
an error.
\item \code{stale} - a position we were in earlier. A capture frame caught mid
animation, or a re-render, shows the board as it was; treating that as a
fresh event would rewind the game.
\item \code{move} - exactly one legal sequence explains it. Accepted.
\item \code{ambiguous} - several sequences explain it, so we cannot say which was
played. Rare in practice but never guessed at.
\item \code{unexplained} - no legal sequence reaches it. Almost always a misread
square; occasionally a jump longer than \code{max_plies}. Rejected either way.
}

While the side to move is still unpinned (the anchor came from a screenshot,
which cannot show whose turn it is) the search is retried from the same
position with the turn flipped. A first move that only makes sense for the
other side is the evidence that settles it.
}
