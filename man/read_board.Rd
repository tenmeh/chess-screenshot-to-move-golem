% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read.R
\name{read_board}
\alias{read_board}
\title{Read a position from a screenshot of a board}
\usage{
read_board(
  image,
  turn = c("w", "b"),
  orientation = c("auto", "white", "black"),
  autocrop = TRUE,
  piece_sets = NULL
)
}
\arguments{
\item{image}{A screenshot: a file path, or a \code{magick-image}.}

\item{turn}{Side to move, \code{"w"} or \code{"b"}. A still image cannot show whose
turn it is, so it has to be told.}

\item{orientation}{One of \code{"auto"}, \code{"white"} or \code{"black"}. \code{"auto"} decides
from where the two armies sit, which is reliable for real positions.}

\item{autocrop}{Trim the surrounding page before splitting. Leave on unless
the screenshot is already cropped tight to the board.}

\item{piece_sets}{Template libraries to match against. Defaults to every set
installed locally.}
}
\value{
A one-row \link{tanmai_position} data frame.
}
\description{
Crops the board out of the screenshot, works out which piece set it is drawn
in, reads the 64 squares, and decides which way round the board is.
}
\details{
Unlike every other way of getting a position, this one can be wrong, so it
reports how sure it is. \code{confident} is \code{FALSE} when the art does not match
any installed piece set well enough to trust - typically a Chess.com or
custom theme. The \code{fen} is still returned in that case, because a
best guess you can correct beats no answer at all, but it is flagged rather
than presented as fact.
}
\examples{
# Render a board and read it straight back.
board <- render_position(fen_to_symbols(
  "rnbqkbnr/pppppppp/8/8/8/8/PPPPPPPP/RNBQKBNR"
))
read_board(board)
}
