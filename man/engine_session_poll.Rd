% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_engine_session.R
\name{engine_session_poll}
\alias{engine_session_poll}
\title{Drain pending engine output and fold it into the session state}
\usage{
engine_session_poll(sess)
}
\arguments{
\item{sess}{An engine session from \code{\link[=engine_session_start]{engine_session_start()}}.}
}
\value{
A list with \code{depth}, \code{lines} (list of PV records ordered by
multipv), \code{fen} and \code{bestmove}, or \code{NULL} if there is no live session.
}
\description{
Non-blocking: reads whatever is buffered and returns immediately, so it is
safe to call from a Shiny observer on a timer.
}
