% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_game_tracker.R
\name{find_move_paths}
\alias{find_move_paths}
\title{Every short legal path from one position to a target placement}
\usage{
find_move_paths(ctx, from_fen, target_placement, max_plies = 2L, cap = 4L)
}
\arguments{
\item{ctx}{A chess.js V8 context from \code{\link[=new_chess_context]{new_chess_context()}}.}

\item{from_fen}{The position we are confident about.}

\item{target_placement}{The placement field just observed.}

\item{max_plies}{Longest sequence to consider.}

\item{cap}{Stop once this many distinct paths are found; the caller only
needs to distinguish none, one and more-than-one.}
}
\value{
A list of character vectors of UCI moves, shortest first.
}
\description{
Searches all legal move sequences up to \code{max_plies} long and returns those
that reach \code{target_placement}. Only the placement is compared, because that
is all a screenshot can show - side to move, castling rights, en passant and
the move clocks are not visible on a board and are instead carried forward
by playing the moves.
}
\details{
Cost grows steeply with depth: two plies is around 1200 positions (~0.1s),
three is around 40000 (~5s). Two is the useful setting - it covers a capture
frame arriving only after the reply - and the default reflects that.
}
