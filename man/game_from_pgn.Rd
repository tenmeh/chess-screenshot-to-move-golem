% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_game_tracker.R
\name{game_from_pgn}
\alias{game_from_pgn}
\title{Build a tracked game from PGN}
\usage{
game_from_pgn(ctx, pgn)
}
\arguments{
\item{ctx}{A chess.js V8 context from \code{\link[=new_chess_context]{new_chess_context()}}.}

\item{pgn}{PGN text, with or without headers.}
}
\value{
A list with \code{game} (a record as from \code{\link[=game_new]{game_new()}}), \code{headers} (named
character, possibly empty) and \code{n_moves}; or a list with \code{error} when the
text could not be parsed.
}
\description{
Parsing PGN properly is a real job - disambiguated SAN, comments, variations,
numeric annotation glyphs, games that start from a set-up position - and
chess.js already does it. It is embedded here anyway, for legality and SAN,
so this hands the text to \code{loadPgn()} and then walks the resulting history to
record the position before every move.
}
\details{
The result is the same shape \code{\link[=game_new]{game_new()}} produces, which is the point: an
imported game is indistinguishable from one watched move by move, so the
evaluation graph, the turning points, the Blunder Radar review and the rating
estimator all work on it with no code of their own.

A \verb{[FEN]} header is honoured, so a study or an endgame that does not begin
from the initial position imports correctly rather than being silently
replayed from the standard start.
}
