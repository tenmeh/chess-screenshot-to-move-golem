% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_blunder_radar.R
\name{blunder_risk}
\alias{blunder_risk}
\title{Assess how likely a human is to go wrong in a position}
\usage{
blunder_risk(
  fen,
  maia,
  min_prob = 0.01,
  movetime_ms = 1200L,
  engine_path = NULL,
  max_loss_cp = 1000
)
}
\arguments{
\item{fen}{Position to assess.}

\item{maia}{A session from \code{\link[=maia_session_start]{maia_session_start()}}.}

\item{min_prob}{Ignore moves a human is very unlikely to play; keeps the
engine work proportional to what actually matters.}

\item{movetime_ms}{Engine time for the evaluation search.}

\item{engine_path}{Optional explicit engine path.}

\item{max_loss_cp}{Ceiling on how bad a single move is allowed to count as.
Past roughly ten pawns a move is simply losing, and without a ceiling a
forced mate (scored in the thousands) would swamp the average and make
\code{risk} unreadable.}
}
\value{
A list with \code{risk} (probability-weighted centipawn loss),
\code{blunder_prob} (chance of losing at least a pawn), \code{sharpness} (how much
the plausible moves disagree in value), \code{best} (the engine's move) and
\code{moves}, a data frame of candidates with \code{prob}, \code{cp}, \code{loss} and
\code{contribution}.
}
\description{
Assess how likely a human is to go wrong in a position
}
